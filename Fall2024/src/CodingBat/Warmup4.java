package CodingBat;

public class Warmup4 {
    //проверяем, что строка имеет хотя бы 2 символа
    //если строка короче, сразу возвращаем false
    //так как она не может начинаться с "hi".
    public static void main(String[] args) {
    }

//    public boolean startHi(String str) {
//        if (str.length() < 2) { // тут узнаем, если длина строки меньше 2 символов, так как строка не может начинаться с HI мы возвращаем false
//            return false;
//        }
//        String firstTwo = str.substring(0,2); //используем метод substring для получения первых двух символов строки
//        // для строки "hello" результат будет HE
//        // для строки "hi there" результат будет HI
//        if (firstTwo.equals("hi")){ // проверяем два символа, если они равны между собой. если равны то ТРУ, если нет то ФОЛС
//            return true;
//        }else {
//            return false;
//        }
    }


